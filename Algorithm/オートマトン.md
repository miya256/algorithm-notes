## 終了状態にたどり着けるか
ある点から終了状態にたどり着けるかどうかは、有向辺を逆向きにしたグラフを考えて、終了状態を始点として、多始点BFSする。
[[BFS]]

## オートマトンDP
どのノードにいるかを状態、遷移をDPの遷移にすればDPできる。
例えば、状態iにいける文字列の個数を数えるみたいな
$$
dp_{n+1}=A\cdot dp_n
$$
状態 $i$ から文字 $c$ で状態 $j$ に行ける場合、 `A[j][i]+=1` のようにしてAをつくる
行列累乗できる。
